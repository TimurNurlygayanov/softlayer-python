<!-- Determines what to show in the browsers titlebar -->

{% if page.header == {{site.project.name}} %}
  {{page.header}} &ndash; {{site.project.tagline}}
{% else %}
  {{page.header}} &ndash; {{site.project.name}}
{% endif %}
